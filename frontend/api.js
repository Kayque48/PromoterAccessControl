[1mdiff --git a/frontend/js/api.js b/frontend/js/api.js[m
[1mindex 69c5876..ac54b17 100644[m
[1m--- a/frontend/js/api.js[m
[1m+++ b/frontend/js/api.js[m
[36m@@ -201,18 +201,21 @@[m [masync function deleteEmpresa(id) {[m
 }[m
 [m
 // Registros de Acesso[m
[31m-async function getRegistrosAcesso() {[m
[31m-    return await apiGet('/registrosacesso');[m
[32m+[m[32m// Retorna lista de promotores ATIVOS (com entrada registrada, sem saída)[m
[32m+[m[32masync function getRegistrosAtivos() {[m
[32m+[m[32m    return await apiGet('/registrosacesso/ativos');[m
 }[m
 [m
[32m+[m[32m// Registra entrada de um promotor[m
 async function registrarEntrada(promotorId, empresaId) {[m
     const usuarioId = parseInt(getCurrentUser()?.id || '', 10);[m
     return await apiPost('/registrosacesso/entrada', { promotorId, empresaId, usuarioId });[m
 }[m
 [m
[31m-async function registrarSaida(registroId) {[m
[32m+[m[32m// Registra saída de um promotor. Requer promotorId + empresaId (não registroId)[m
[32m+[m[32masync function registrarSaida(promotorId, empresaId) {[m
     const usuarioId = parseInt(getCurrentUser()?.id || '', 10);[m
[31m-    return await apiPost(`/registrosacesso/${registroId}/saida`, { usuarioId });[m
[32m+[m[32m    return await apiPost('/registrosacesso/saida', { promotorId, empresaId, usuarioId });[m
 }[m
 [m
 // Relatórios[m
[1mdiff --git a/frontend/js/registro-ponto.js b/frontend/js/registro-ponto.js[m
[1mindex bfd406f..48a9c40 100644[m
[1m--- a/frontend/js/registro-ponto.js[m
[1m+++ b/frontend/js/registro-ponto.js[m
[36m@@ -1 +1,3 @@[m
[31m-// registro-ponto.js - Gerencia registro de ponto\n\nlet registrosAbertos = [];\n\ndocument.addEventListener('DOMContentLoaded', async function() {\n    // Verifica autenticação\n    requireAuth();\n    \n    // Carrega promotores\n    await carregarPromotoresSelect();\n    \n    // Carrega registros abertos\n    await carregarRegistrosAbertos();\n    \n    // Listeners\n    const btnEntrada = document.getElementById('btnEntrada');\n    if (btnEntrada) {\n        btnEntrada.addEventListener('click', handleRegistrarEntrada);\n    }\n});\n\nasync function carregarPromotoresSelect() {\n    try {\n        const promotores = await getPromotores();\n        const select = document.getElementById('promotor');\n        if (select) {\n            promotores.forEach(prom => {\n                const option = document.createElement('option');\n                option.value = prom.id;\n                option.textContent = prom.nome;\n                select.appendChild(option);\n            });\n        }\n    } catch (error) {\n        console.error('Erro ao carregar promotores:', error);\n    }\n}\n\nasync function handleRegistrarEntrada() {\n    const promoterSelect = document.getElementById('promotor');\n    const promoterId = promoterSelect.value;\n    \n    if (!promoterId) {\n        alert('Selecione um promotor');\n        return;\n    }\n    \n    try {\n        // Obtém empresa do promotor (você pode precisar adicionar isso)\n        const promoter = await getPromotor(promoterId);\n        const empresaId = promoter.empresaId;\n        \n        const resultado = await registrarEntrada(promoterId, empresaId);\n        \n        alert('Entrada registrada com sucesso!');\n        promoterSelect.value = '';\n        \n        // Recarrega registros\n        await carregarRegistrosAbertos();\n    } catch (error) {\n        console.error('Erro ao registrar entrada:', error);\n        alert('Erro ao registrar entrada');\n    }\n}\n\nasync function carregarRegistrosAbertos() {\n    try {\n        const registros = await getRegistrosAcesso();\n        registrosAbertos = registros.filter(r => !r.saida); // Apenas os que não têm saída\n        renderizarRegistrosAbertos();\n    } catch (error) {\n        console.error('Erro ao carregar registros:', error);\n    }\n}\n\nfunction renderizarRegistrosAbertos() {\n    const container = document.getElementById('listaRegistrosAbertos');\n    if (!container) return;\n    \n    if (registrosAbertos.length === 0) {\n        container.innerHTML = '<p class=\"text-muted\">Nenhum promotor em atendimento no momento.</p>';\n        return;\n    }\n    \n    container.innerHTML = '';\n    \n    const tabela = document.createElement('div');\n    tabela.className = 'table-responsive';\n    tabela.innerHTML = `\n        <table class=\"table table-hover mb-0\">\n            <thead>\n                <tr>\n                    <th>Promotor</th>\n                    <th>Entrada</th>\n                    <th>Duração</th>\n                    <th>Ação</th>\n                </tr>\n            </thead>\n            <tbody>\n                ${registrosAbertos.map(reg => `\n                    <tr>\n                        <td>${reg.promotorNome}</td>\n                        <td>${new Date(reg.entrada).toLocaleTimeString('pt-BR')}</td>\n                        <td>${calcularDuracao(reg.entrada)}</td>\n                        <td>\n                            <button class=\"btn btn-sm btn-primary\" onclick="handleSaida(${reg.id})"\">Saída</button>\n                        </td>\n                    </tr>\n                `).join('')}\n            </tbody>\n        </table>\n    `;\n    \n    container.appendChild(tabela);\n}\n\nasync function handleSaida(registroId) {\n    if (!confirm('Registrar saída?')) return;\n    \n    try {\n        await registrarSaida(registroId);\n        alert('Saída registrada com sucesso!');\n        await carregarRegistrosAbertos();\n    } catch (error) {\n        console.error('Erro ao registrar saída:', error);\n        alert('Erro ao registrar saída');\n    }\n}\n\nfunction calcularDuracao(entrada) {\n    const agora = new Date();\n    const entradaDate = new Date(entrada);\n    const diff = agora - entradaDate;\n    \n    const horas = Math.floor(diff / 3600000);\n    const minutos = Math.floor((diff % 3600000) / 60000);\n    \n    return `${horas}h ${minutos}m`;\n}\n[m
\ No newline at end of file[m
[32m+[m[32m// registro-ponto.js - Gerencia registro de ponto[m
[32m+[m
[32m+[m[32mlet registrosAbertos = [];\n\ndocument.addEventListener('DOMContentLoaded', async function() {\n    // Verifica autenticação\n    requireAuth();\n    \n    // Carrega promotores\n    await carregarPromotoresSelect();\n    \n    // Carrega registros abertos\n    await carregarRegistrosAbertos();\n    \n    // Listeners\n    const btnEntrada = document.getElementById('btnEntrada');\n    if (btnEntrada) {\n        btnEntrada.addEventListener('click', handleRegistrarEntrada);\n    }\n});\n\nasync function carregarPromotoresSelect() {\n    try {\n        const promotores = await getPromotores();\n        const select = document.getElementById('promotor');\n        if (select) {\n            promotores.forEach(prom => {\n                const option = document.createElement('option');\n                option.value = prom.id;\n                option.textContent = prom.nome;\n                select.appendChild(option);\n            });\n        }\n    } catch (error) {\n        console.error('Erro ao carregar promotores:', error);\n    }\n}\n\nasync function handleRegistrarEntrada() {\n    const promoterSelect = document.getElementById('promotor');\n    const promoterId = promoterSelect.value;\n    \n    if (!promoterId) {\n        alert('Selecione um promotor');\n        return;\n    }\n    \n    try {\n        // Obtém empresa do promotor (você pode precisar adicionar isso)\n        const promoter = await getPromotor(promoterId);\n        const empresaId = promoter.empresaId;\n        \n        const resultado = await registrarEntrada(promoterId, empresaId);\n        \n        alert('Entrada registrada com sucesso!');\n        promoterSelect.value = '';\n        \n        // Recarrega registros\n        await carregarRegistrosAbertos();\n    } catch (error) {\n        console.error('Erro ao registrar entrada:', error);\n        alert('Erro ao registrar entrada');\n    }\n}\n\nasync function carregarRegistrosAbertos() {\n    try {\n        const registros = await getRegistrosAtivos();\n        registrosAbertos = registros;\n        renderizarRegistrosAbertos();\n    } catch (error) {\n        console.error('Erro ao carregar registros:', error);\n    }\n}\n\nfunction renderizarRegistrosAbertos() {\n    const container = document.getElementById('listaRegistrosAbertos');\n    if (!container) return;\n    \n    if (registrosAbertos.length === 0) {\n        container.innerHTML = '<p class=\"text-muted\">Nenhum promotor em atendimento no momento.</p>';\n        return;\n    }\n    \n    container.innerHTML = '';\n    \n    const tabela = document.createElement('div');\n    tabela.className = 'table-responsive';\n    tabela.innerHTML = `\n        <table class=\"table table-hover mb-0\">\n            <thead>\n                <tr>\n                    <th>Promotor</th>\n                    <th>Entrada</th>\n                    <th>Duração</th>\n                    <th>Ação</th>\n                </tr>\n            </thead>\n            <tbody>\n                ${registrosAbertos.map(reg => `\n                    <tr>\n                        <td>${reg.promotorNome}</td>\n                        <td>${new Date(reg.entrada).toLocaleTimeString('pt-BR')}</td>\n                        <td>${calcularDuracao(reg.entrada)}</td>\n                        <td>\n                            <button class=\"btn btn-sm btn-primary\" onclick="handleSaida(${reg.id})"\">Saída</button>\n                        </td>\n                    </tr>\n                `).join('')}\n            </tbody>\n        </table>\n    `;\n    \n    container.appendChild(tabela);\n}\n\nasync function handleSaida(promotorId, empresaId) {\n    if (!confirm('Registrar saída?')) return;\n    \n    try {\n        await registrarSaida(registroId);\n        alert('Saída registrada com sucesso!');\n        await carregarRegistrosAbertos();\n    } catch (error) {\n        console.error('Erro ao registrar saída:', error);\n        alert('Erro ao registrar saída');\n    }\n}\n\nfunction calcularDuracao(entrada) {\n    const agora = new Date();\n    const entradaDate = new Date(entrada);\n    const diff = agora - entradaDate;\n    \n    const horas = Math.floor(diff / 3600000);\n    const minutos = Math.floor((diff % 3600000) / 60000);\n    \n    return `${horas}h ${minutos}m`;\n}\n[m
\ No newline at end of file[m
